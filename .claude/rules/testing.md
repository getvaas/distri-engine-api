Always run tests using the script configured in `.sdd.json` at `paths.run_tests`. Never run test framework commands directly (e.g., test runners, coverage tools, or package manager test scripts). The configured test scripts use Docker to ensure consistent, reproducible results regardless of host dependencies. If the configured script does not exist, fall back to whatever test command is available in the project.
